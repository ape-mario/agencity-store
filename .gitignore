/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.env
.env*.local
.DS_Store

# hosted job-spec documents (the activation route writes here)
/.agenc/

# dev/build artifacts (the kept deliverables live alongside in proof/*.json + .png)
proof/*.log
proof/homepage.html

# editor/agent workspace state
/.zcode/

# reference/comparison implementations from other agents (local study only, never committed)
/tests/


